import React, { Component } from 'react';
import { TextInput, StyleSheet } from 'react-native'
const UnderlineInput = (props) => <TextInput {...props} underlineColorAndroid="transparent" style={[styles.underlineText, props.inputStyle]} multiline={true}/>

const styles = StyleSheet.create({
    underlineText: {
        alignSelf: 'center',
        color: 'black',
        fontSize: 18,
        flex: 1,
        borderBottomWidth: 1,
        borderColor: '#666',
        paddingVertical: 0,
        textAlign: 'center'
    },
});

export default UnderlineInput;